#ifndef SELECTOR_H
#define SELECTOR_H

#include <iostream>
#include <Windows.h>
#include <direct.h>

std::string getCurrentDirectory();
std::string SingleFileSelector();
std::string GetParentPath(std::string);

#endif

